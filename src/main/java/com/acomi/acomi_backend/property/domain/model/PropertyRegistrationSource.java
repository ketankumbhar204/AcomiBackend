package com.acomi.acomi_backend.property.domain.model;

/** Where a property lead came from. Server-assigned; never accepted from the client. */
public enum PropertyRegistrationSource {
    PUBLIC_WEBSITE,
    ADMIN
}
